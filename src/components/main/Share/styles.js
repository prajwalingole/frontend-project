import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    btn: {
        marginRight: '10px',
        color: '#b388ff'
    },
    container: {
        marginTop: '10px'
    }
}));