import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    ftxt: {
        fontWeight: 'bold',
    },
    stxt: {
        spacing: '2px'
    },
    btn: {
        marginRight: '10px'
    },
    container: {
        marginTop: '10px'
    }
}));